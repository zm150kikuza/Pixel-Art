# Pixel Art Maker v2

A frontend-only pixel-art converter for static hosting and Google Sites embeds.

## Deploy

Upload the contents of this folder to a GitHub repository and enable **Settings → Pages → Deploy from a branch → main / root**. Embed the published URL in Google Sites using **Insert → Embed → By URL**.

No server, API key, or upload storage is used. Images remain in the visitor's browser.
